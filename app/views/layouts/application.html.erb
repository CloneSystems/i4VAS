<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8" />
  <title><%= content_for?(:title) ? yield(:title) : "OpenVAS" %></title>
  <%= stylesheet_link_tag "application" %>
  <%= javascript_include_tag 'jquery', 'jquery.min', 'jquery_ujs' %>
  <%= csrf_meta_tag %>
  <%= yield(:head) %>
</head>
<body>
<div id="container">
	<div id="nav_wrapper">
		<% if user_signed_in? %>
			<div class="logo"><h1>OpenVAS</h1></div>
			<div class="nav">
				<%= link_to 'Home', root_path %>
				 | 
				<%= link_to "Sign Out", destroy_user_session_path %>
				 | 
				<% Time.zone = 'Eastern Time (US & Canada)' %>
				timeout at: 
				<%= current_user.class.timeout_in.ago.in_time_zone.strftime("%a %b %d, %Y %H:%M:%S %Z") %>
				<%#= current_user.class.inspect %>
				&larr; <span class="nav_text">user:</span> <%= current_user.username %>
				<hr />
				<%= link_to 'Tasks', tasks_path %>
				 | 
				<%= link_to 'New Task', new_task_path %>
				 | 
				Notes
				 | 
				Overrides
				&larr; <span class="nav_text">scan management</span>
				<hr />
				Scan Configs
				 | 
				<%= link_to 'Targets', scan_targets_path %>
				 | 
				Credentials
				 | 
				Agents
				 | 
				Escalators
				 | 
				<%= link_to "Schedules", schedules_path %>
				 | 
				Report Formats
				 | 
				Slaves
				&larr; <span class="nav_text">configuration</span>
				<hr />
				Users
				 | 
				NVT Feed
				 | 
				Settings
				&larr; <span class="nav_text">administration</span>
			</div>
		<% end %>
	</div>
	<div class="clear"></div>
	<div>
		<% flash.each do |name, msg| %>
		  <%= content_tag :div, msg, :id => "flash_#{name}" %>
		<% end %>
		<%= content_tag :h1, yield(:title) if show_title? %>
		<%= yield %>
	</div>
</div>
</body>
</html>
